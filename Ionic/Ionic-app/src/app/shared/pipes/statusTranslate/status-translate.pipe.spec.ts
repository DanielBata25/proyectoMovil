import { StatusTranslatePipesPipe } from './status-translate.pipe';

describe('StatusTranslatePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTranslatePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
